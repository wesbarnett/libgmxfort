prefix=MYPREFIX
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
sharedlibdir=${libdir}
includedir=${prefix}/include

Name: libgmxfort
Description: Fortran toolkit for analyzing GROMACS simulations
Version: MYVERSION

Requires:
Libs: -L${libdir} -L${sharedlibdir} -lgmxfort
Cflags: -I${includedir}
